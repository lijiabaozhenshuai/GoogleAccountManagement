========================================
  谷歌账号管理系统 - 使用说明
========================================

【系统要求】
- Windows 10 或更高版本
- MySQL 8.0 或更高版本
- 4GB 内存

========================================
🚀 开发者使用
========================================

进入 package 目录，双击 "打包.bat"

[1] 安装依赖
    安装 Python 依赖包

[2] 启动程序（测试）
    访问 http://localhost:5000

[3] 打包成 EXE
    生成: dist\谷歌账号管理系统\

[4] 制作用户部署包
    生成: 谷歌账号管理系统_部署包.zip

========================================
👤 用户使用
========================================

【安装步骤】

1. 安装 MySQL
   下载: https://dev.mysql.com/downloads/installer/
   设置 root 密码（建议: 123456）

2. 创建数据库
   打开 MySQL 命令行:
   CREATE DATABASE google_account CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

3. 配置系统
   - 复制 config.json.example 为 config.json
   - 修改 MySQL 密码等配置

4. 启动程序
   - 双击 "启动.bat"
   - 浏览器访问 http://localhost:5000

========================================
⚙️ 配置文件
========================================

config.json 示例:

{
  "mysql": {
    "host": "localhost",
    "port": 3306,
    "user": "root",
    "password": "123456",
    "database": "google_account"
  },
  "hubstudio": {
    "base_url": "http://localhost:6873",
    "app_id": "your_app_id",
    "app_secret": "your_app_secret"
  },
  "channel_avatar_path": "D:\\avatars",
  "appeal_text_path": "D:\\申诉文件.xlsx"
}

注意: Windows 路径使用 \\ 或 /

========================================
📁 临时文件目录
========================================

所有临时文件存储在:
- app_data\   应用数据
- instance\   数据库文件

========================================
❓ 常见问题
========================================

Q: 打包后运行闪退？
A: 使用 "启动.bat" 查看错误信息
   确保 config.json 存在且正确

Q: 无法连接数据库？
A: net start MySQL (启动MySQL服务)
   检查 config.json 中的密码

Q: 缺少某个模块？
A: 在 build_config.py 的 hiddenimports 中添加

Q: 打包体积太大？
A: 在 build_config.py 的 excludes 中添加不需要的包

========================================
